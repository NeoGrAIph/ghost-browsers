{
  "name": "ghost-browsers",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "lint": "pnpm -C apps/ui lint",
    "test": "pnpm -C apps/ui test",
    "build": "pnpm -C apps/ui build"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "eslint": "^9.10.0"
  }
}
