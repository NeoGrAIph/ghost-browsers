FROM node:20-alpine AS build
WORKDIR /app

# Install dependencies separately to avoid copying host node_modules into the image.
COPY ui/package*.json ./
RUN npm install

# Copy only the source required for the production build.
COPY ui/src ./src
COPY ui/index.html ./
COPY ui/tsconfig.json ./
COPY ui/vite.config.ts ./
RUN npm run build

FROM nginx:1.27-alpine
COPY docker/nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
